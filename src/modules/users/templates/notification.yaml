roleChanged: |
  {{ admin.fullName }} ({{ admin.email }}) updated {{ user.fullName }}'s ({{ user.email }}) roles set:
  <br>
  Previous:
  {{#previousRoleNames}}
    <code>{{.}}</code>
  {{/previousRoleNames}}
  <br>
  New:
  {{#targetRoleNames}}
    <code>{{.}}</code>
  {{/targetRoleNames}}
